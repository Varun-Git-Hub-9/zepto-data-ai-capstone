# Module 2 - Analytics Pipeline

This module uses the Titanic dataset to perform data profiling, cleaning, exploratory analysis, classification, model tuning, imbalance handling, and a regression side-task.

## Dataset

The Titanic dataset is loaded using Seaborn:

```python
sns.load_dataset("titanic")
